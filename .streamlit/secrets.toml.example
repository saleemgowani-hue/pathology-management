# Copy this file to .streamlit/secrets.toml and fill in your own values
# for local development. On Streamlit Community Cloud, do NOT upload
# secrets.toml — instead paste these same keys into your app's
# Settings -> Secrets in the Streamlit Cloud dashboard.

DATABASE_URL = "postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require"
